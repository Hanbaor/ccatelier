---
title: "盛最多水的容器"
date: "2026-09-13 10:54:35"
updated: "2026-09-20 16:33:22"
source_url: "https://blog.csdn.net/m0_68856756/article/details/165003255"
source_id: "165003255"
author: "CC"
categories: ["算法"]
tags: ["LeetCode", "双指针"]
permalink: "hot100/005/"
series: "hot100"
series_order: 5
topic: "双指针"
difficulty: "中等"
---

<span>LeetCode #11 · Container With Most Water</span>

| 项目 | 内容 |
| --- | --- |
| 难度 | <font>中等</font> |
| 专题 | 双指针 |
| 标签 | 贪心、数组、双指针 |
| 题目链接 | [打开 LeetCode 原题](https://leetcode.cn/problems/container-with-most-water/) |

## 题目要求

给定一个长度为 `n` 的整数数组 `height` 。有 `n` 条垂线，第 `i` 条线的两个端点是 `(i, 0)` 和 `(i, height[i])` 。

找出其中的两条线，使得它们与 `x` 轴共同构成的容器可以容纳最多的水。

返回容器可以储存的最大水量。

说明：你不能倾斜容器。

 

示例 1：

```text
输入：[1,8,6,2,5,4,8,3,7]
输出：49 
解释：图中垂直线代表输入数组 [1,8,6,2,5,4,8,3,7]。在此情况下，容器能够容纳水（表示为蓝色部分）的最大值为 49。
```

示例 2：

```text
输入：height = [1,1]
输出：1
```

 

提示：

	
- `n == height.length`

	
- `2 <= n <= 10^5`

	
- `0 <= height[i] <= 10^4`

## 代码实现

```cpp
class Solution {
public:
    int maxArea(vector<int>& height) {
        int left=0,right=height.size()-1;
        int ans=min(height[left],height[right])*(right-left);
        while(left<right){
            if(height[left]<=height[right]) left++;
            else right--;
            ans=max(min(height[left],height[right])*(right-left),ans);
        }
        return ans;
    }
};
```

## 个人解析

> 在这里补充：解题思路、关键推导、时间复杂度、空间复杂度、易错点与复盘记录。

[返回目录](/series/hot100/)
