---
title: "回文链表"
date: "2026-09-13 10:54:35"
updated: "2026-09-20 16:33:22"
source_url: "https://blog.csdn.net/m0_68856756/article/details/165003255"
source_id: "165003255"
author: "CC"
categories: ["算法"]
tags: ["LeetCode", "链表"]
permalink: "hot100/024/"
series: "hot100"
series_order: 24
topic: "链表"
difficulty: "简单"
---

<span>LeetCode #234 · Palindrome Linked List</span>

| 项目 | 内容 |
| --- | --- |
| 难度 | <font>简单</font> |
| 专题 | 链表 |
| 标签 | 栈、递归、链表、双指针 |
| 题目链接 | [打开 LeetCode 原题](https://leetcode.cn/problems/palindrome-linked-list/) |

## 题目要求

给你一个单链表的头节点 `head` ，请你判断该链表是否为回文链表。如果是，返回 `true` ；否则，返回 `false` 。

 

示例 1：

```text
输入：head = [1,2,2,1]
输出：true
```

示例 2：

```text
输入：head = [1,2]
输出：false
```

 

提示：

	
- 链表中节点数目在范围`[1, 10^5]` 内

	
- `0 <= Node.val <= 9`

 

进阶：你能否用 `O(n)` 时间复杂度和 `O(1)` 空间复杂度解决此题？

## 代码实现

```cpp
/**
 * Definition for singly-linked list.
 * struct ListNode {
 *     int val;
 *     ListNode *next;
 *     ListNode() : val(0), next(nullptr) {}
 *     ListNode(int x) : val(x), next(nullptr) {}
 *     ListNode(int x, ListNode *next) : val(x), next(next) {}
 * };
 */
class Solution {
    ListNode* frontP;
public:
    bool isPalindrome(ListNode* head) {
        frontP=head;
        return dfs(head);
    }

    bool dfs(ListNode* head){
        if(head!=nullptr){
            if(!dfs(head->next)) return false;
            if(head->val!=frontP->val) return false;
            frontP=frontP->next;
        }
        return true;
    }
};
```

## 个人解析

> 在这里补充：解题思路、关键推导、时间复杂度、空间复杂度、易错点与复盘记录。

[返回目录](/series/hot100/)
