---
title: "找到字符串中所有字母异位词"
date: "2026-09-13 10:54:35"
updated: "2026-09-20 16:33:22"
source_url: "https://blog.csdn.net/m0_68856756/article/details/165003255"
source_id: "165003255"
author: "CC"
categories: ["算法"]
tags: ["LeetCode", "滑动窗口"]
permalink: "hot100/009/"
series: "hot100"
series_order: 9
topic: "滑动窗口"
difficulty: "中等"
---

<span>LeetCode #438 · Find All Anagrams in a String</span>

| 项目 | 内容 |
| --- | --- |
| 难度 | <font>中等</font> |
| 专题 | 滑动窗口 |
| 标签 | 哈希表、字符串、滑动窗口 |
| 题目链接 | [打开 LeetCode 原题](https://leetcode.cn/problems/find-all-anagrams-in-a-string/) |

## 题目要求

给定两个字符串 `s` 和 `p`，找到 `s` 中所有 `p` 的 异位词 的子串，返回这些子串的起始索引。不考虑答案输出的顺序。

 

示例 1:

```text
输入: s = "cbaebabacd", p = "abc"
输出: [0,6]
解释:
起始索引等于 0 的子串是 "cba", 它是 "abc" 的异位词。
起始索引等于 6 的子串是 "bac", 它是 "abc" 的异位词。
```

 示例 2:

```text
输入: s = "abab", p = "ab"
输出: [0,1,2]
解释:
起始索引等于 0 的子串是 "ab", 它是 "ab" 的异位词。
起始索引等于 1 的子串是 "ba", 它是 "ab" 的异位词。
起始索引等于 2 的子串是 "ab", 它是 "ab" 的异位词。
```

 

提示:

	
- `1 <= s.length, p.length <= 3 * 10^4`

	
- `s` 和 `p` 仅包含小写字母

## 代码实现

```cpp
class Solution {
public:
    vector<int> findAnagrams(string s, string p) {
        unordered_map<char,int> mp;
        vector<int> ans;
        for(int i=0;i<p.size();i++) mp[p[i]]++;
        for(int l=0,r=0;r<s.size();r++){
            mp[s[r]]--;
            while(mp[s[r]]<0){
                mp[s[l]]++;l++;
            }
            if(r-l+1==p.size()) ans.push_back(l);
        }
        return ans;
    }
};
```

## 个人解析

> 在这里补充：解题思路、关键推导、时间复杂度、空间复杂度、易错点与复盘记录。

[返回目录](/series/hot100/)
