# Nijika Redefine Implementation Plan

> Execute in this session with superpowers:executing-plans and a final independent review.

**Goal:** Implement the approved Live House design in the real Hexo blog on `nijika`, retaining Redefine and adding polished theme-consistent interaction details.
**Architecture:** Pin the upstream theme. Apply project-owned template overrides during Hexo generation; reuse upstream writing and rendering facilities. Serve real static routes, progressive client effects and generated search data.
**Tech Stack:** Hexo 8, Redefine 2.9, EJS, CSS, browser ES modules, Node test runner.
**Spec:** `docs/superpowers/specs/2026-09-23-nijika-design.md`

## Global constraints

- Branch `nijika` from main; no node_modules edits or production deployment.
- Preserve actual user article content/permalink; official character art only.
- Native links and content work without JS. Reduce text; don't invent public content.
- Coarse pointers and reduced motion receive appropriate fallbacks.

## Review focus

1. Long/Unicode article titles, empty collections and multiple list pages.
2. Search results containing HTML-like strings or unsafe URLs; failed index loads.
3. Keyboard input, focus restoration, browser back and modified link clicks.
4. Persisted light/motion preferences and missing/blocked localStorage or clipboard.
5. Small/short viewports, no JS, cursor cleanup and audio/dialog lifecycle.

## Tasks

- [x] 1. Build integration: add pinned dependencies/config, `scripts/nijika.js`, project overrides and route-generation helpers. First write a generated-site integration test and confirm it fails on the baseline. Keep article sources. Register root cover separately from `notes/`; categories/tags/archive remain generated by Hexo.
- [x] 2. Templates and visual design: create shared head/header/footer/dialogs; cover, real notes/list/post, project/research/life/about and 404 templates. Reuse approved local official assets/fonts and Redefine post TOC/writing output. Build and inspect real routes.
- [x] 3. Interaction: modules for shell/theme/motion/entry, generated-index search, optional drums, cursor/click effects and reading tools. Use native links with progressive entrance transition; safe DOM text for search; effects with bounded DOM and cleanup.
- [x] 4. Verification and refinement: run integration fixture build, syntax and route/assets checks; browser inspect desktop/tablet/phone and actual content, keyboard/dialogs/search/back/cursor/audio. Correct every material defect. Get fresh whole-branch review, resolve findings, record evidence and commit completed work on nijika.

## Commands

- Baseline/build: `node node_modules/hexo/bin/hexo generate`
- Verification: `node --test tests/site.test.cjs`
- Local server: `node node_modules/hexo/bin/hexo server -p 4318`
- Review: compare branch and working tree with `main`, inspect source and actual generated pages.

## Progress

- Branch created from `main` b9c4357; original working tree clean. Baseline Hexo generation passed.
- Ruling: work in the user-requested branch in the current repository. User explicitly requested implementation after approving V5; proceed without repeating design approval gates.
- Environment: `npm` on PATH is an empty system shim. Use Node with `C:/Program Files/nodejs/node_modules/npm/bin/npm-cli.js` for dependency operations and Hexo's local CLI for builds.
- Completed genuine Redefine integration with project-owned overrides, official local images, five real section routes, Markdown content, search, reading tools and detailed pointer/motion controls.
- Final clean build: 267 files; Node tests 8/8; isolated 10-post fixture checks passed; JS syntax and whitespace checks passed. Browser and review evidence: `docs/superpowers/nijika-verification.md`.
- Independent review findings resolved, including Chinese IME handling and SearchDB custom-permalink normalization. No remaining Critical or Important findings. Main and production remain unchanged.
